# Frontend Scaffold

This directory is reserved for the CrowdShield AI dashboard.

Recommended structure:
- src/components: Reusable UI widgets (cards, charts, health badges)
- src/pages: Route-level dashboard pages
- src/services: API and WebSocket clients

Implement the dashboard using service contracts in backend/shared/contracts/events.py.